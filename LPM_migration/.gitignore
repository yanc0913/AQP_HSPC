# Archived old versions (kept locally, not part of the repo)
_archive/

# Python cache
__pycache__/
*.pyc

# Pipeline outputs / data (written under paths in config.yaml, but ignore if any land locally)
results/
data/
*.tif
*.tiff

# OS / IDE junk
.DS_Store
Thumbs.db
.vscode/
.idea/
.ipynb_checkpoints/

# Machine-specific configs: they hold real data paths. Copy the .example.yaml
# files and edit the two paths under `project` - every analysis parameter lives
# in the examples, so nothing is lost by not tracking these.
/config.yaml
/config_midline_v3.yaml
