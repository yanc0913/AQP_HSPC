# Machine-specific config (real data paths) — copy config.example.yaml -> config.yaml
config.yaml

# Archived old versions (kept locally, not part of the repo)
_archive/

# Python cache
__pycache__/
*.pyc

# Pipeline outputs / data
results/
data/
*.tif
*.tiff

# OS / IDE junk
.DS_Store
Thumbs.db
.vscode/
.idea/
.ipynb_checkpoints/
